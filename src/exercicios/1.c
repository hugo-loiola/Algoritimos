#include <stdio.h>

int main()
{
  char caracter;
  int inteiro;
  float flutuante;
  double dobro;

  printf("Char: %d\n", sizeof(caracter));
  printf("Int: %d\n", sizeof(inteiro));
  printf("Float: %d\n", sizeof(flutuante));
  printf("Double: %d\n", sizeof(dobro));
  return 0;
}